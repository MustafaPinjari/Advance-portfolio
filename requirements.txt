Django>=4.0,<5.0
whitenoise>=6.0
markdown
dj_database_url
djangorestframework
psycopg2-binary

# ASGI dependencies (for Uvicorn/Gunicorn deployment)
gunicorn
uvicorn

# Database support (if using PostgreSQL, uncomment the next line)
# psycopg2-binary

# If using environment variables for configuration
django-environ
